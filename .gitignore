# Reference material that is not ours to publish.
# Third-party skill packages carry their own git history and licences.
/Skills/
# Inspiration videos are third-party reference, ~29 MB, and not part of the product.
/inspiratie/
# The brand guide lives in the repo as frontend/docs/visual-style-guide.png instead.
/styleguide/

# macOS
.DS_Store
**/.DS_Store

# Local environment. Never commit real secrets; .env.example is the template.
.env
.env.*
!.env.example

# Docker volumes
/.docker-data/

# Local agent and editor config
/.claude/
